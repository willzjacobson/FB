var router = require('express').Router();
module.exports = router;
var mongoose = require('mongoose');

// schema.methods.findWithSimilarHobbies = function() {
// 	return this.find({ type: {"$in" : this.hobbies}})
// 	.then(function(users) {

// 	})
// 	.then(null, next);
// };